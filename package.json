{
  "name": "memolock",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "native:dev": "neu run",
    "native:build": "npm run build && neu build --release",
    "native:portable": "npm run build && neu build --clean --embed-resources",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@fontsource/merriweather": "5.3.0",
    "@neutralinojs/lib": "^6.3.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.0.6",
    "vue": "^3.5.18"
  },
  "devDependencies": {
    "@neutralinojs/neu": "^11.7.2",
    "@types/node": "^24.1.0",
    "@vue/test-utils": "^2.4.6",
    "jsdom": "^26.1.0",
    "typescript": "~5.8.3",
    "vitest": "^3.2.4",
    "vue-tsc": "^3.0.4"
  }
}
